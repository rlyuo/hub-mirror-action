name: image_move_rlyuo
on:
  push:
    branches: 
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: image_move_rlyuo
      uses: Yikun/hub-mirror-action@master 
      with:
        src: github/rlyuo
        dst: gitee/ryuo
        dst_key: ${{ secrets.GITEE_PRIVATE_KEY }} 
        dst_token:  ${{ secrets.GITEE_TOKEN }} 
        account_type: user
        clone_style: "ssh" 
        debug: true 
        force_update: true 
        white_list: "llvm-project" 
        timeout: '600000s' 
